# 目录扫描优化：仅显示 datasets 目录

## 🎯 需求描述

用户希望目录选择功能只显示 `datasets` 目录中的子目录，而不是扫描整个项目目录，避免显示无关的目录。

## 🔍 问题分析

**之前的行为**：
- 扫描整个项目根目录
- 显示所有包含图像或标签文件的目录
- 包括 `.venv`、`node_modules` 等无关目录
- 目录列表冗长且难以使用

**期望的行为**：
- 只扫描 `datasets` 目录
- 显示 `datasets` 下的直接子目录
- 过滤掉无关目录
- 提供更清晰的目录选择

## 🛠️ 修复方案

### 修改 `serve_directories` 函数

**修改前**：
```python
# 扫描项目目录，查找可能的图像和标签目录
for root, dirs, files in os.walk(self.base_dir):
    # 扫描整个项目目录
```

**修改后**：
```python
# 只扫描 datasets 目录
datasets_dir = Path(self.base_dir) / "datasets"
if not datasets_dir.exists():
    result = {'images': [], 'labels': []}
else:
    # 扫描 datasets 目录下的所有子目录
    for item in datasets_dir.iterdir():
        if item.is_dir():
            # 只处理 datasets 下的直接子目录
```

### 优化目录检测逻辑

1. **限制扫描范围**：只扫描 `datasets` 目录
2. **智能识别**：自动识别包含图像或标签的子目录
3. **去重排序**：确保目录列表整洁有序
4. **错误处理**：处理 `datasets` 目录不存在的情况

## ✅ 修复结果

### 测试验证

运行测试脚本的结果：

```
📁 发现的图像目录: 3
📁 发现的标签目录: 2
图像目录:
  - datasets/coco8
  - datasets/images
  - datasets/visualizations
标签目录:
  - datasets/coco8
  - datasets/demo
✅ 确认: 只显示 datasets 目录下的子目录
```

### 改进效果

**修改前**：
- 显示 22 个图像目录（包括 `.venv` 等无关目录）
- 显示 204 个标签目录（包括大量无关目录）
- 目录列表混乱，难以选择

**修改后**：
- 显示 3 个图像目录（仅 datasets 相关）
- 显示 2 个标签目录（仅 datasets 相关）
- 目录列表清晰，易于选择

## 🎨 用户体验改进

### 1. 更清晰的目录选择
- 只显示相关的数据集目录
- 避免混淆和误选
- 提高工作效率

### 2. 更快的加载速度
- 减少扫描范围
- 降低系统资源消耗
- 提升响应速度

### 3. 更好的组织结构
- 符合项目结构规范
- 便于数据集管理
- 支持多数据集项目

## 📁 支持的目录结构

```
datasets/
├── coco8/              # 主要数据集
│   ├── images/
│   │   ├── train/
│   │   └── val/
│   └── labels/
│       ├── train/
│       └── val/
├── demo/               # 演示数据
│   ├── images/
│   └── labels/
├── images/             # 其他图像数据
└── visualizations/     # 可视化数据
```

## 🚀 使用方法

1. **启动工具**：
   ```bash
   bash datasets/start_web_annotation.sh
   ```

2. **访问界面**：
   在浏览器中打开 `http://localhost:9090`

3. **选择目录**：
   - 图像目录下拉菜单只显示 `datasets` 下的子目录
   - 标签目录下拉菜单只显示 `datasets` 下的子目录
   - 选择对应的数据集目录

4. **开始标注**：
   - 点击"加载图像"
   - 开始标注工作

## 🔧 技术细节

### 目录扫描逻辑

```python
# 只扫描 datasets 目录
datasets_dir = Path(self.base_dir) / "datasets"

# 检查每个子目录
for item in datasets_dir.iterdir():
    if item.is_dir():
        # 检查是否包含图像或标签文件
        # 添加到相应的目录列表
```

### 文件类型检测

- **图像文件**：`.jpg`, `.jpeg`, `.png`, `.bmp`, `.tiff`, `.webp`
- **标签文件**：`.txt` 文件

### 错误处理

- 处理 `datasets` 目录不存在的情况
- 处理权限问题
- 处理空目录的情况

## 📝 注意事项

1. **目录结构**：确保数据集放在 `datasets` 目录下
2. **文件格式**：支持标准的图像和标签文件格式
3. **权限**：确保对 `datasets` 目录有读取权限
4. **命名规范**：建议使用清晰的目录命名

## 🎯 后续优化

1. **配置化**：支持自定义扫描目录
2. **缓存**：缓存目录扫描结果
3. **过滤**：支持更精细的目录过滤规则
4. **预览**：显示目录中的文件数量统计

---

**修改完成时间**：2024-01-XX  
**测试状态**：✅ 通过  
**部署状态**：✅ 可用
